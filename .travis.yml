language: node_js
node_js: '8'
cache: yarn

stages:
  - lint
  - test

jobs:
  include:
    - stage: lint
      install: npm install
      script: npm run lint
    - stage: test
      install: npm install
      script: npm run test
